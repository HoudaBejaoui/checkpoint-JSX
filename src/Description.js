import React from "react";
// Functional component for displaying product description.
const Description = ({ description }) => (
  <p style={{ fontStyle: "italic", marginBottom: "10px" }}>{description}</p>
);

export default Description;